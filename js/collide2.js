col2 = (x,y,colData,data,blocks)=>{
	console.log(colData,data,blocks);

	// for (var i = 0; i < 3; i++) {
	// 	if (i ==0) {
	// 		for(i=0;i<1;i++){
	// 			if (blocks[i].x == colData[0].y && blocks[i].y == y)
	// 				console.log("1 A");
	// 		}
	// 		for (var i = 0; i < data.length; i++) {
	// 			if(i>0)
	// 				if (data[i].y == y && data[i].x == x)
	// 					console.log("1 B");
	// 		}
	// 	}else if (i ==1) {
	// 		for(i=0;i<1;i++){
	// 			if (blocks[i].x == colData[1].y && blocks[i].y == y)
	// 				console.log("2 A");
	// 		}
	// 		for (var i = 0; i < data.length; i++) {
	// 			if(i>0)
	// 				if (data[i].y == y && data[i].x == x)
	// 					console.log("2 B");
	// 		}
	// 	}else{
	// 		for(i=0;i<1;i++){
	// 			if (blocks[i].x == colData[2].y && blocks[i].y == y)
	// 				console.log("3 A");
	// 		}
	// 		for (var i = 0; i < data.length; i++) {
	// 			if(i>0)
	// 				if (data[i].y == y && data[i].x == x)
	// 					console.log("4 B");
	// 		}
	// 	}
	// }
}
